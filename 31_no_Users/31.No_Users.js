"use strict";
let userNames = ["admin", "jumaid", "zeeshan", "kamran", "daniyal"];
if (userNames.length === 0) {
    console.log("we need find some users!");
}
else {
    userNames = [];
    console.log("all user name have been removed." + userNames.length);
}
